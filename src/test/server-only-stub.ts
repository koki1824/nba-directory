/**
 * テスト中の `server-only` の代わり。
 *
 * 本物は、クライアント側から読み込まれたときに例外を投げて
 * サーバー専用のコード（DBの接続情報など）がブラウザへ紛れ込むのを防ぐ。
 * DBにつなぐテストはサーバー側の想定なので、ここでは何もしない実装に差し替える。
 *
 * 差し替えているのは vitest.integration.config.ts の alias。
 * 本番のビルドではこのファイルは使われず、本物の仕掛けがそのまま効く。
 */
export {};
