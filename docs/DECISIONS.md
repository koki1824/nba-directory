# DECISIONS — オーナー承認済みの決定事項

［保留］項目としてオーナー承認が必要だったものの、確定した内容を記録します。
**このファイルの決定は、以後の実装で勝手に変えません。** 変更する場合は行を書き換え、変更理由と影響範囲を残してください。

---

## W0-6 ［保留］4項目（決定日 2026-07-29 / オーナー承認済み）

### 1. シーズン当時年齢の基準日 = **そのシーズンの開幕日**

- **決定**: 「シーズン当時の年齢」は、**当該シーズンのレギュラーシーズン開幕日**時点の満年齢とする。
- 出典: 要件定義書 4.3「シーズン当時年齢の基準日は未確定。開幕日または固定日をユーザー承認後に採用する。」

**実装への影響（W1-5 / W1-7 で必ず反映すること）**

- `seasons` テーブルに **開幕日カラム（`regular_season_start_date`）を必ず持たせる。** 年齢計算がこの列に依存するため、後付けにしない。
- 開幕日は reference seed（`W1-7`）で全シーズン分を投入する。API から取得できる保証がないため、**seed を正とする**。
- ロックアウト・パンデミック等で開幕が変則だったシーズン（1998-99 / 2011-12 / 2019-20 / 2020-21 など）は、
  実際の開幕日を個別に確認して投入する。ここを推測値で埋めないこと。
- 開幕日が未投入のシーズンでは、**当時年齢を表示しない**（推測で出さない）。
  値としては `N/A` 扱いとする（オーバーライド §8「0 / NULL / N/A / Not calculated を区別する」）。
- 実装は `seasons.opening_date` を `age_reference_date` にコピーして使う（[11_OPEN_QUESTIONS.md](./11_OPEN_QUESTIONS.md) §11.1 Q-a）。

**既知のトレードオフ（オーナー了承済み）**

Basketball Reference など英語圏の主要スタッツサイトは「シーズン終了年の2月1日時点」の年齢を採用しています。
本サイトは開幕日基準なので、**選手によっては他サイトと年齢が1歳ずれて見えます。**
`W2-13`（seed数値の抜き取り確認）や `W4-7`（実データ検証）で他サイトと突き合わせた際、
年齢の差異は不具合ではなく仕様です。混乱を避けるため、**選手詳細ページに「年齢はシーズン開幕日時点」と明記する**こと。

### 2. 選手比較の同時比較人数 = **4人**（実装順は2人レイアウトを優先）

- **決定**: 上限は **4人**。2026-07-28 のオーナー決定（推奨案2人からの変更）を正式仕様として維持する。
- 出典: [11_OPEN_QUESTIONS.md](./11_OPEN_QUESTIONS.md) §11.1 Q-b、§11.6。
- モックの食い違い（トップ「最大5人」／比較「最大3名」／トレー「2/5名」）は**すべて4人に統一**する。

**実装への影響（`W2-7` / `W2-11`）**

- 比較ページは**人数に応じてレイアウトを切り替える**。2人用と4人用を別ページにしない（URLが分裂するため）。

  | 人数 | レイアウト |
  | --- | --- |
  | 2人 | モック `02_compare.jpg` どおりの**対比レイアウト**（左右対置・双方向パーセンタイルバー） |
  | 3〜4人 | **表形式**（選手を列・指標を行）。パーセンタイルは各選手1本の横バー |

- **実装順は2人レイアウトを先に完成させる。** 3〜4人の表形式はその後に足す。
  §11.6 が「8/12チェックポイントで遅延した場合は2人レイアウトを優先し、3〜4人対応を公開後に回す」と定めており、
  最初からその順で作れば、遅延しても縮退の判断だけで済み、作り直しが発生しない。
- URL は `?p=` を最大4つまで受け付け、**5つ以上は無視して先頭4件に丸める**（エラーにしない）。
- モバイルは4人だと横幅が足りないため、指標行を主体にした横スクロール表 + 主要4指標のみ上部固定。
- データ層は人数に依存しない。比較対象は「固定スロット」ではなく**選手IDの配列**として扱い、
  上限は表示層の定数（`MAX_COMPARE_PLAYERS = 4`）で制御する。

### 3. 仮サイト名 slug = **`nba-directory`**

- **決定**: URL・Vercelプロジェクト名・リポジトリ名をすべて `nba-directory` で統一する（オーナー決定 2026-08-02）。
- 出典: 要件定義書「正式サイト名・ドメイン・最終ロゴデータは後日決定」［保留］。

> **⚠️ この項目は [11_OPEN_QUESTIONS.md](./11_OPEN_QUESTIONS.md) §11.1 Q-c を上書きします。**
> 同文書は 2026-07-28 時点で `nba-meikan`（第2候補 `court-archive`）と記録していますが、
> 2026-08-02 のオーナー決定により `nba-directory` に変更されました。
> 11_OPEN_QUESTIONS.md は当時の記録として原文のまま残しています。**決定の正はこのファイルです。**

**実装への影響**

- あくまで**仮**。正式サイト名の決定後に変更しうる。
- 表示名（`<title>` やヘッダー）にこの slug を直接使わないこと。表示名は差し替え可能な1箇所に集約する。
- ロゴ（`W1-4`）は、要件定義書の指示どおり**サイト名が変わってもシンボルを流用できる構造**にする。

### 4. プレーオフのランキング規定 = **全選手表示＋試合数明示**

- **決定**: プレーオフのランキングでは**独自の最低試合数を設けず、全選手を表示**し、**試合数を必ず併記**する。
- 出典: 要件定義書「プレーオフにレギュラーシーズンの58試合規定を流用しない。公式基準がない指標は全選手表示 + 試合数明示とする。」

**実装への影響**

- `ranking_rules`（`W1-5` / `W3-6`）は **RS と PO で別レコード**として持つ。PO 側は最低条件なしを表現できる形にする。
- ランキング画面（`W2-10`）は、PO のとき「最低条件なし・試合数を併記」と画面上に明示する。
- 公式基準が存在する PO 指標が見つかった場合は、この決定を上書きせず**追加の行として記録**してから実装する。
- レギュラーシーズン側の規定（58試合等）と公式例外の扱いは `W3-10` の対象であり、本決定の範囲外。

---

## W0-7 追加確認事項 Q1〜Q10（決定日 2026-07-28 / オーナー承認済み）

全文は [11_OPEN_QUESTIONS.md](./11_OPEN_QUESTIONS.md) §11.2 にあります。実装時に効くものだけ再掲します。

| # | 決定 | 実装への影響 |
| --- | --- | --- |
| Q2 | BPM / VORP は**取得できたら表示**。`ProviderCapabilities.metrics` に無ければ**列ごと出さない** | 空欄を並べない。列の出し分けを `W1-9` の型に組み込む |
| Q3 | パーセンタイルの母集団は**規定到達者のみ**。画面に母集団と最低条件を明示 | `W2-6` / `W2-7` |
| Q5 | ローテーション／デプスチャートは **9/1非表示**。自動推測しない | — |
| Q6 | 順位変動（前シーズン比）は **9/1非表示**。`ranking_snapshots` はスキーマのみ用意 | `W1-5` |
| Q7 | ヘッダーの「コラム」ナビは **9/1非表示**。ナビ定義は1ファイルに集約し、公開後に1行で出せる形に | `W1-10` |
| Q8 | 「引退選手」タブは **9/1非表示** | — |
| Q9 | 問い合わせは**メール転送のみ**。`inquiries` はスキーマのみ | `W1-5` / `W3-8` |
| Q10 | 比較表の**数値は濃紺 `#0F1D2D`**、**赤 `#B23A2E` は強調・罫線・選択状態に限定** | `W1-3` デザイントークン。コントラスト確保のため |

---

## 未決定として残っているもの

### Q4 選手名の日本語（カタカナ）表記の全件方針 — **期限 Week 3**

- seed 20〜30名は**手入力**（`name_ja_state = human_verified`）で進める。ここは決まっている。
- **全件（API連携成功時の1,200名規模）をどうするかは未決定。** 「英語名のまま」「機械的にカタカナ化」「上位N名のみ手作業」のいずれか。
- スキーマは `players.full_name_ja` + `name_ja_state`（未設定 / 機械 / 人手確認済み）で受けるため、**どの結論でも作り直しは起きない**。
- **9/1 の seed 公開シナリオでは表面化しない。** Phase 3 が成功した場合にのみ問題化する。
- 詳細: [08_FEASIBILITY.md](./08_FEASIBILITY.md) §8.6、[11_OPEN_QUESTIONS.md](./11_OPEN_QUESTIONS.md) §11.2 Q4。

### ~~W0-1 API契約条件の書面回答~~ — **2026-09-05 に解決（→ §8）**

7項目すべてOK。未決定として残っているのは **Q4（選手名の日本語表記の全件方針）のみ**です。

---

## 再開時の決定（決定日 2026-09-05 / オーナー承認済み）

8/2 から 9/5 まで作業が停止し、当初の公開日 9/1 が経過したため、再設定しました。

### 5. 新しい公開日 = **2026年10月4日（日）**

- 残タスクは公開スコープで52件。当初計画の Sprint 1〜4 相当がほぼ丸々残っているため、4週間を確保した。
- **この日付は再び動かさない。** 遅れた場合は ROADMAP §削減ラダー の順に機能を削る。
- オーナー作業は残り**合計 約4.5時間**（Phase 3 を公開後に回したぶん、実データ検証1〜2hが公開スコープから外れた）。

### 6. Phase 3（API連携）を **10/4 の公開スコープから外す**

> **2026-09-05 追記:** 当初この判断の理由は「`W0-1` の回答が期限までに届かなかったこと」でしたが、
> 同日中に返信を受領し、7項目すべてOKと確認できました（§8）。
> **それでもスコープ外の判断は変えません。理由が規約から時間に変わっただけです。**

- **外す理由は時間。** 残り4週間に、当初5週間分だった Sprint 1〜4 と Phase 3 の両方は入らない。
- 10/4 は seed データで公開し、「データ準備中のサンプル・非公式サイト」を明示する（`W5-10`）。
- **着手時期:** 10/4 公開の直後。
  切り替えは `DATA_PROVIDER` と `PROVIDER_PERSISTENCE_ALLOWED` の変更のみで済むため、公開後の着手でも作り直しは発生しない。

### 7. 開発の運用方法

**目的はオーナーの操作を最小にすること。** 停止期間中に失われたのは作業時間だけで、成果物は GitHub に push してあったため無傷だった。この経験を踏まえて次のとおり固定する。

| 項目 | 決定 | 理由 |
| --- | --- | --- |
| コードの反映 | **`main` に直接 push**。PR は挟まない | 1人開発でレビュアーがいないため PR は儀式になる。CI が門番を務める |
| デプロイ | **Vercel の GitHub 連携で自動** | push のたびに公開URLが更新される。オーナーの操作はゼロ |
| ファイルの受け渡し | **GitHub に一本化**。デスクトップ経由のアップロードは行わない | AI 側は使い捨てのコンテナで動くため、push したものだけが残る。実際 8/2〜9/5 の停止中にコンテナは作り直されたが、リポジトリの内容は無傷だった |
| オーナーの役割 | **公開URLを見て OK / NG を言う**、それだけ | 判断に専念してもらい、作業は AI 側で完結させる |

### 8. `W0-1` API契約条件 — **7項目すべてOK**（オーナー判断 2026-09-05）

- BALLDONTLIE から返信を受領。**オーナーが本文を読み、7項目すべて問題なしと判断**した。
- 判定はオーナーの読解に基づくものであり、AI側で条文を逐条確認したわけではない。
  後日この判断を見直す必要が生じた場合は、返信本文に立ち返ること。

**実装への影響**

- **自社DBへの永続保存が可能。** `08_FEASIBILITY.md` §8.3 が「最大の未知」と呼んでいた関門②が解消した。
  `W1-5` の全21テーブルは、キャッシュ主体ではなく**永続保存を前提に確定してよい**。
- 本番で外部Providerを使う際は `PROVIDER_PERSISTENCE_ALLOWED=true` を設定する
  （既定は `false`。許諾が確認できるまで保存させないための起動時ガード。`W1-9`）。
- **Phase 3 の公開スコープ外という判断は変わらない。** 外した理由は規約ではなく時間である
  （残り4週間に5週間分の作業は入らない）。10/4 公開の直後に着手する。

### 9. `engines.node` の書き方 — Vercel が解釈できる形にする

> **2026-09-05 訂正:** この変更を「Vercel のデプロイ失敗の原因」として記録していましたが、**誤りでした。**
> 実際のログでは `npm install` も型チェックも成功しており、`engine-strict` は関係ありません。
> 真の原因は環境変数の空文字（→ [REFERENCES.md](./REFERENCES.md) 項目11）です。
> ただし `"22.x"` への変更自体は Vercel が Node を選べる正しい書き方なので、そのまま維持します。

- `">=22.22.2"` のような範囲表記は Vercel が解釈できないため、`"22.x"` に変更した。
- パッチ版の下限（22.22.2 = jsdom/undici の要求）は `engines` では縛れなくなったため、
  **`src/test/setup.ts` の起動時チェックで見張る**。古い Node ではテストが明確なメッセージで即座に止まる。

---

## `W1-12` Phase 0 承認時の決定（決定日 2026-09-06 / オーナー承認済み）

**`W1-12` は承認されました。** Phase 0（データの持ち方・URLの形・見た目の土台）はこれで確定です。
以降、これらを変更する場合は「作り直しになる」ことを承知した上での判断になります。

### 10. シーズン開幕日 = **オーナーが出典を確認して入力する**（案2）

`docs/SCHEMA.md` で提示した3案のうち、**案2** を選択。
私は案1（10/4 では年齢を出さない）を推していましたが、オーナーの判断で案2とします。

- 対象は **2015-16 〜 2025-26 の11シーズン分**のみ
- **入力するまでは年齢を表示しません。** ビューがそう作ってあるので、誤った数字は出ません
- **AIが記憶から埋めることは引き続き禁止**（§1 の決定は維持）。
  入力される値はオーナーが出典を確認したものであり、これは「推測値で埋めない」に反しません

**入力の方法は2つあり、早い方を使えます。**

| | 方法 | いつできるか |
| --- | --- | --- |
| a | オーナーが日付をこの会話で伝え、AIがマイグレーションに書く | **いつでも** |
| b | 管理画面から入力する | `W3-3` 完成後（Sprint 3） |

**a を推奨します。** 11件だけなので画面を作るまでもなく、先に入れておけば
選手ページの実装時点から年齢が出た状態で確認できます。
b の管理画面は、公開後に新しいシーズンを追加するときのために別途用意します。

### 11. トップページに「押せる」動きを付ける（オーナー指摘）

- **サイト名とヘッダーの文字を大きく・太く**する
- **入口カードはカーソルを重ねると浮き上がり、押すと沈む**

実装上の判断:

- **カード全体をリンクにした。** 文字だけがリンクだと指で押せる範囲が狭い
- **浮きは 2px、影も控えめ。** Direction A は紙の資料に近い見た目のため、
  大きく浮かせると禁止事項の「AI生成感の強いUI」に寄る
- **`:hover` は `@media (hover: hover)` の中だけに書いた。**
  スマホはタップを hover と解釈することがあり、指を離しても浮いたままになる。
  タップの手応えは `:active`（押している間だけ沈む）で出す
- **OSの「視差効果を減らす」設定を尊重して動かさない。** 動きで気分が悪くなる人がいるため
- **動きだけに頼らない。** 色の変化も併せて付ける

### 12. **Phase 3（API連携）を 10/4 公開スコープに戻す**（決定日 2026-09-06 / オーナー判断）

`W2-1`（seedデータ）の作成にあたり、「10/4 に公開される数値をどうするか」を確認したところ、
**API連携を前倒しする**という判断になりました。§6 の決定（Phase 3 をスコープ外にする）を**取り消します。**

**なぜ確認が必要だったか**

オーバーライド v3 §7 は seed を「実在選手20〜30名の実数値」と定め、同時に
「AIが生成した推測値をそのまま公開しない」とも定めています。この2つは両立しません。
私はNBAの実際の成績を正確に思い出せないため、実在選手の名前で書けば
**それらしいが誤った数字を、実在の人物の記録として公開する**ことになります。
「サンプルデータ」と明示しても、誤情報である事実は変わりません。

Phase 3 を戻せば実データが入るので、この問題は根本から消えます。

**この決定に伴って決まること**

1. **seedデータの位置づけが変わる**（`W2-1`）
   - 公開する数値ではなく、**開発とテストのための材料**になる
   - 実在の人物と紛れないよう、**架空の選手名**で作る
   - 境界ケース（途中移籍・欠損・PO未出場・同姓同名）は従来どおり全部入れる
   - **実データが間に合わなかった場合の退避先**でもある（`W4-10` `W4-11` の判定は残す）

2. **削る機能**（§削減ラダー の1番・2番。順序は決定済みなので、そのまま適用する）
   - 画像パイプライン（`W5-1`〜`W5-3` `W5-5`）→ 公開後。全選手を代替表示で公開する
   - チーム比較（`W2-9`）→ 公開後。チーム詳細と年代別ロスターは残す
   - **選手比較・選手詳細・選手一覧には手を付けない。** ここが中核仮説

3. **同期はGitHub Actionsで動かす**
   - 私の作業環境から外部APIへ接続できない（プロキシが遮断する）ため、
     ここで実データを取得して確認することはできない
   - GitHub Actions は外部に接続できるので、そこで取得してSupabaseへ書き込む
   - 私は**記録した応答を使って手元で検証**し、本物のAPIとの疎通はCI上で確認する

**⚠️ 確認が要ること（オーナー作業）**

- **BALLDONTLIE のAPIキー**を GitHub Secrets に登録（`BALLDONTLIE_API_KEY`）
- **契約プランで必要なデータが取れるか。** 無料枠では選手一覧しか取れず、
  成績（season averages）は有料、ということがあり得ます。
  **お金が絡むので、私は勝手に決めません。** 必要なプランが分かった時点でお知らせします
- キーが無い間、同期ワークフローは**緑のまま何もせず終了**します（マイグレーションと同じ作り）。
  開発は止まりません

### 13. ⚠️ 未決: シーズン成績を「1試合平均」と「合計」のどちらで取り込むか

**2026-09-06 に判明。オーナーの判断が要ります（お金が絡む可能性があります）。**

**何が起きたか**

`W4-2` の取り込みを、応答を真似た偽サーバーで通しで動かしたところ、
BALLDONTLIE の `season_averages` が返すのは**1試合平均**（小数）でした。
一方このサイトの `player_season_stats` は**シーズン合計の本数**（整数）を持ちます。

**なぜ合計で持っているか**

このプロジェクトの中核の決めごとです（オーバーライド v3 §8）。

- 「試投0本」と「0%」を区別するため。合計の本数が無いと区別できません
- 通算の成功率を正しく出すため。**率の平均は誤り**で、合計から計算する必要があります

**平均をそのまま入れるとどうなるか**

- 「8.4本決めた」という**存在しない記録**になります
- キャリア通算が「平均の足し算」になり、まったく違う数字になります

**いまの状態**

平均が来たら**取り込まずに止めます**。誤った数字をサイトに載せるより、
入らないほうがましだからです（実際に止まることを確認済み）。

**選択肢**

| | 方法 | 正確さ | 費用・手間 |
| --- | --- | --- | --- |
| a | 合計を返すエンドポイントを使う | ◎ 公式記録そのもの | 契約プランによる。**要確認** |
| b | 1試合ごとの記録を取得して合計する | ◎ 正確 | リクエスト数が大幅に増える（選手数×試合数）。**上位プランが必要になる可能性** |
| c | 平均 × 試合数で合計を推定する | △ 丸め誤差が乗る | 追加費用なし |
| d | 保存する内容を「平均」に変える | ✗ | **中核の決めごとを崩すため推奨しません** |

**c について:** 推定値を実数として保存すると、通算の成功率がわずかにずれます。
このサイトは数値の正しさを売りにしているので、私は勧めません。
ただし「10/4に間に合わせる」ことを優先するなら選択肢には入ります。
その場合は**推定値である旨をデータ出典のページに明記**します。

**次にすること:** APIキーを登録して `npm run probe:api`（GitHub Actions の
「APIの応答を確認」）を実行すると、**契約プランで何が取れるかが分かります。**
それを見てから決めるのが確実です。
