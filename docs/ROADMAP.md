# ROADMAP — 2026/10/4 公開まで

**ゴール:** 公開URLが存在し、**実在のNBAファン1名が実際に選手比較機能を操作した**日 = **2026年10月4日（日）**

> **公開日を 9/1 → 10/4 へ再設定しました（オーナー決定 2026-09-05）。**
> 8/2 から 9/5 まで作業が停止し、9/1 は経過しました。新しい日付は**再び動かしません。**
> **2026-09-06 更新:** Phase 3（API連携）を**公開スコープに戻しました**。
> 代わりに画像パイプラインとチーム比較を公開後に回します（→ §Phase 3 / [DECISIONS.md](./DECISIONS.md) §12）。

**進捗: 完了 18 / 全 71**（うち 10/4 公開スコープは 60。最終更新 2026-09-05）
**日付は動かさない。** 遅れたら §削減ラダー に沿って機能を削る。

## 現在地

- 本日 **2026-09-06**。Sprint 1 の2日目。**残り28日 / 公開スコープの残り42タスク。**
- 完了18件: `W0-1`（API契約確認）、`W0-2` `W0-3` `W0-4`（Supabase / Vercel のアカウントと連携）、`W0-6` `W0-7`（［保留］項目とQ1〜Q10の決定）、
  `W0-8`（設計文書一式）、`W1-1`（プロジェクト初期化）、`W1-2`（テスト基盤とCI）、`W1-3`（デザイントークンとUIプリミティブ）、`W1-4`（ロゴ）、`W1-11`（初回デプロイ）、`W1-5`（DBスキーマ）、`W1-6`（ビュー6種）、`W1-7`（基礎データ）、`W1-8`（型の自動生成）、`W1-9`（Provider Adapter）、`W1-10`（ルーティング骨格）
- **8/2 から 9/5 まで停止していました。** リポジトリは無傷で、CI も `main` で緑のままです
- **`W0-1` 完了。** BALLDONTLIE から返信を受領し、**7項目すべてOK**とオーナーが判断（2026-09-05）。
  自社DBへの永続保存が可能になったため、`W1-5` のDBスキーマは**保存できる前提で確定**できます。
  **2026-09-06、Phase 3 を公開スコープに戻しました**（→ [DECISIONS.md](./DECISIONS.md) §12）。
  seed を実在選手の名前で作ると誤った数字を実在の人物の記録として公開することになるため、実データを入れる方を選びました
- **`W1-11` 完了（9/5）。Vercel のデプロイが緑になりました。** 期限 9/13 のチェックポイントを初日で回収。
  以後は `main` に push するたびに自動でデプロイされます
- **`W1-5` 完了（9/6）。** 21テーブル・RLS・制約を実装し、手元と CI の両方で検証済み。
  レビュー用の日本語まとめは [SCHEMA.md](./SCHEMA.md)
- **自動開発モードで進行中**（オーナー決定 2026-09-06）。`[AI]` タスクを連続して進め、
  `W1-12`（Phase 0 承認）で止まって通知する。5時間ごとに自動で再開する
- **`W1-12`（Phase 0 承認）が通りました（9/6）。Sprint 1 完了、Sprint 2 に入ります。**
  設計は確定。開幕日は案2（オーナーが入力）。見た目の指摘2件は対応済み（→ [DECISIONS.md](./DECISIONS.md) §10・§11）
- **⏳ オーナー待ち（急ぎではない）:** 2015-16〜2025-26 の**開幕日11件**。
  この会話で日付を伝えていただければ、その場で反映します。**それまで年齢は表示されません**（誤った数字は出ません）
- **ランキングの不具合を1件見つけて直しました（9/6・`0004`）。**
  「全シーズン共通の既定」と「シーズン個別の規定」が両方あると、規定の照合が2件に当たり、
  ランキングに同じ選手が2回出て、規定未到達の選手にも順位が付いていた。
  手元で再現（1選手が8行 / 1試合しか出ていない選手が規定到達扱い）→ 修正 → 再現しないことを確認。
  同じ壊れ方を捕まえる検査を `verify-views.mjs` に追加済み（26項目）

## 運用（2026-09-05 決定）

- **コードは `main` に直接 push する。** PR は挟まない。CI が門番を務める
- **Vercel は GitHub 連携で自動デプロイ。** push するたびに公開URLが更新される
- **オーナーの操作は原則「公開URLを見て OK / NG を言う」だけ。** それ以外の手作業を増やさない
- ファイルの受け渡しは GitHub に一本化する。デスクトップ経由のアップロードは行わない

### 既知の注意点

- **Supabase の無料プランは、一定期間使わないとプロジェクトが一時停止する。**
  2026-09-05 に実際に停止していた（8/2 以降アクセスがなかったため）。
  ダッシュボードの **Restore / Resume** で復帰でき、**データも接続情報も失われない。作り直す必要はない。**
  スキーマ適用後は CI やアプリから定期的にアクセスが発生するので、起きにくくなる。
- **`next start` のプロセスが残っていると、再ビルドしても古い画面が配信され続ける。**
  画面で確認するときは、見ているものが最新のビルドかを疑うこと（→ [REFERENCES.md](./REFERENCES.md) 項目12）。
- **Supabase の接続文字列は、ダッシュボード上部の「Connect」ボタンから取る。**
  Project Settings の中に Database の項目は無い（UIが変更された）。
  さらに「Session pooler」はトップに出ておらず、**`Direct — Connection string` のタイルの中**に
  Direct / Transaction pooler / Session pooler の3つが並んでいる。
  **UIのラベルは変わりうるので、文字列そのもので見分けること:**

  | 文字列の特徴 | 判定 |
  | --- | --- |
  | `pooler.supabase.com` を含み `:5432` で終わる | ✅ Session pooler（これを使う） |
  | `pooler.supabase.com` を含み `:6543` で終わる | ❌ Transaction pooler |
  | `db.〇〇.supabase.co` を含む | ❌ Direct connection（IPv6のみ。GitHub ActionsはIPv4） |

  取得画面に **Type** の選択がある場合は **URI** を選ぶこと。Node.js や JDBC を選ぶと
  コード片やパラメータの羅列になり、1本の文字列にならない。
  文字列中の `[YOUR-PASSWORD]` に入れるのは、**プロジェクト作成時に設定した
  データベースのパスワード**。supabase.com のログインパスワードでも API Keys でもない。
  作成時に一度しか表示されないため、控えていなければ **Reset database password** で再発行する
  （再発行は、そのパスワードを使っている設定が増える前ほど影響が小さい）。
  `scripts/migrate.mjs` が起動時にこれらを判定し、向いていない文字列なら警告する。

## ラベル

`[AI]` Claude単独で完了 ／ `[AI→承認]` AIが作りオーナーは承認のみ ／ `[オーナー]` オーナーにしかできない

---

## Week 0 — 7/27〜8/2（完了）

**残っているのは `W0-5`（Node確認）と `W0-9`（代替プロバイダ調査）の2件のみです。**

- [x] `W0-1` **[オーナー]** BALLDONTLIE等の契約条件を**書面で**確認依頼（1〜2h + 返信待ち）★最長納期
      ※2026-09-05 に返信を受領。**オーナー判断で7項目すべてOK**（→ [DECISIONS.md](./DECISIONS.md) §8）
      確認7項目: 商用表示可否 / **自社DB保存可否** / キャッシュ保持期間 / 再配布 / 解約後のデータ保持 / 出典表示義務 / レート制限・取得可能年度
- [x] `W0-2` **[オーナー]** Supabaseアカウント + プロジェクト作成（20分）→ DB URL / anon key / service role key を控える
- [x] `W0-3` **[オーナー]** Vercelアカウント作成（10分・GitHubログイン可）
- [x] `W0-4` **[オーナー]** GitHubリポジトリへのVercel連携を許可（15分）※リポジトリ自体は作成済み
- [ ] `W0-5` **[オーナー]** Node.js **22.22.2以上**のインストール確認（15分）
      ※当初「20以上」でしたが、テスト環境(jsdom 30 / undici 8)がNode 22.22.2以上を要求するため引き上げました。
      バージョンは `.nvmrc` に固定済みです（`nvm use` で切り替わります）。
- [x] `W0-6` **[オーナー]** ［保留］4項目の決定（30分）→ 決定内容は [DECISIONS.md](./DECISIONS.md)
      季節年齢の基準日 / 比較上限人数 / 仮サイト名slug / プレーオフランキングの表示ルール
- [x] `W0-7` **[オーナー]** 追加確認事項への回答（20分）→ [11_OPEN_QUESTIONS.md](./11_OPEN_QUESTIONS.md) の Q4〜Q10
      ※2026-07-28に決定済み。Q4（選手名の日本語表記の全件方針）のみ Week 3 まで持ち越し
- [x] `W0-8` **[AI]** Phase 0 設計文書一式の提出（本 `docs/`）
- [ ] `W0-9` **[AI]** 代替データプロバイダの候補調査（保存不可だった場合に備える）

---

## Sprint 1 — 9/5〜9/13（Phase 0 完了 → Phase 1 着手）

**完了条件:** スキーマ・Providerインターフェース・URL・トークンをオーナーが承認済み
**チェックポイント 9/13:** Phase 0承認済み・Vercelに空アプリがデプロイ済み

- [x] `W1-1` **[AI]** Next.js 15 + TypeScript strict + Tailwind + ESLint/Prettier 初期化、`.gitignore` / `.env.example`
- [x] `W1-2` **[AI]** Vitest + Playwright 導入、GitHub Actions CI（typecheck / lint / unit）
- [x] `W1-3` **[AI]** デザイントークン実装 + UIプリミティブ（Button / Select / Tabs / Table / StatBar / MissingValue）
      → 見本帳を `/styleguide` に用意（noindex）。オーナーはここで実物を確認できる
- [x] `W1-4` **[AI]** ロゴをSVGで実装（グリッド・線幅・16px時の視認性調整）
      → 16/20/24/32/48/64px で描画して確認。24px未満は簡略版に自動で切り替える。ファビコンも同形
- [x] `W1-5` **[AI]** `0001_init.sql`：**全21テーブル**（10/4に使わない favorites・page_seo 等も含む）+ enum + index + RLS
      → レビュー用の日本語まとめは [SCHEMA.md](./SCHEMA.md)。`npm run verify:schema` で制約が効くことを毎回検証（16項目）
- [x] `W1-6` **[AI]** ビュー6種（override実効値 / 率・36分換算 / キャリア / rosters / rankings / percentiles）
      → `npm run verify:views` で数値の正しさを毎回検証（23項目）。キャリア率がシーズン率の平均になっていないことを含む
- [x] `W1-7` **[AI]** reference seed（seasons / metric_definitions / ranking_rules / data_sources）
      → 「取り決め」のみ投入。**開幕日などの事実は入れていない**（推測で埋めない・DECISIONS §1）
- [x] `W1-8` **[AI]** DBの構造から型を生成 → `src/db/types.generated.ts` + CIで差分検査
      ※`supabase gen types` は Docker を要求し手元で回せないため、自作の生成器に変更（→ [REFERENCES.md](./REFERENCES.md) 項目13）
- [x] `W1-9` **[AI]** Provider Adapter 型定義 + SeedProvider骨格 + registry + **永続保存許諾の起動時ガード**
      → 許諾が無い外部プロバイダは起動時に例外を投げて止める。seedは自前なので許諾を問わない
- [x] `W1-10` **[AI]** 全URLのルーティング骨格 + 共通レイアウト + robots / sitemap
      → 全15URLがE2Eで疎通確認済み。比較ページと見本帳は noindex。ナビ定義は1ファイルに集約（Q7）
- [x] `W1-11` **[AI]** Vercel初回デプロイ（空アプリが公開URLで見える）← **9/13 チェックポイント達成（9/5）**
- [x] `W1-12` **[オーナー]** **Phase 0成果物レビューと承認 ← 2026-09-06 承認**
      → 手引き: [PHASE0_REVIEW.md](./PHASE0_REVIEW.md) / 決定: [DECISIONS.md](./DECISIONS.md) §10・§11
      - 設計（データの持ち方・URLの形）は **OK**。これで確定
      - 開幕日は **案2（オーナーが入力）**。入力までは年齢を表示しない
      - 見た目に指摘2件 → 対応済み（サイト名を大きく太く／入口カードに浮き上がる動き）
- **Sprint 1 完了。** `[AI]` タスクは全て終わり、承認も取れました（9/6）

---

## Sprint 2 — 9/14〜9/20（Phase 1：UI全体）

**完了条件:** 主要画面がseedデータで動作。モバイル含む
**チェックポイント 9/20:** 選手比較がseedデータで動作

- [x] `W2-1` **[AI]** seedデータ作成：**架空の選手**20〜30名 / 3チーム以上 / 複数シーズン / 境界ケース全部入り
      （途中移籍・欠損・RSとPO両方・PO未出場・同姓同名候補）
      → **公開する数値ではなく、開発とテストの材料**（[DECISIONS.md](./DECISIONS.md) §12）。
      実在の人物と紛れないよう架空名にする。実データが間に合わない場合の退避先も兼ねる
      → 24名 / 4チーム / 3シーズン。`npm run verify:seed` で境界ケースが欠けていないか毎回検査（20項目）。
      数値は生成器が内訳から計算するので、得点と内訳が食い違うことがない
- [x] `W2-2` **[AI]** `domain/stats` 実装 + unitテスト（平均 / 36分換算 / eFG% / TS% / **キャリア集計** / stint整合）→ T2 T3 T12
      → 式がDBのビューと2か所にあるため、共通の期待値（`fixtures/stat-parity.json`）で
      両方を突き合わせる検査を用意した（`npm run verify:parity`・CIに組み込み済み）
- [x] `W2-3` **[AI]** `domain/missing` 実装 + unitテスト（0 / NULL / N/A / Not calculated）→ T6
      → 割り算は必ず `divide()` を通す。分母0は「算出不可」であって0%ではない
- [ ] `W2-4` **[AI]** トップページ
- [ ] `W2-5` **[AI]** 選手一覧（検索・絞り込み・ソート・比較トレー・URLクエリ反映）→ T1
- [ ] `W2-6` **[AI]** 選手詳細（概要 / シーズン別 / キャリア / PO / 受賞歴 / 所属履歴 / パーセンタイル）
- [ ] `W2-7` **[AI]** **選手比較** ★中核仮説。最後まで削らない
- [ ] `W2-8` **[AI]** チーム一覧・チーム詳細・年代別ロスター → T4
- [ ] ~~`W2-9` **[AI]** チーム比較~~ → **公開後に回す**（削減ラダー2番。Phase 3 前倒しの原資）
- [ ] `W2-10` **[AI]** ランキング（規定到達判定つき）
- [ ] `W2-11` **[AI]** モバイル情報設計（比較の主要値を上部固定 / 詳細を縦積み / bottom sheet / sticky tray）→ T11
- [ ] `W2-12` **[オーナー]** モック7枚との突き合わせレビュー・差分指摘（30分 × 2回）
- [ ] ~~`W2-13` **[オーナー]** seed数値の抜き取り確認~~ → **不要になりました**。
      seed は架空の選手による開発用の材料になり、公開されないためです（[DECISIONS.md](./DECISIONS.md) §12）。
      **公開する数値の確認は `W4-7`（API実データの抜き取り検証）に一本化します**

---

## Sprint 3 — 9/21〜9/27（Phase 2：DB運用基盤・管理画面）

**完了条件:** 管理画面・override・sync logs・ランキング規定が動作
**チェックポイント 9/27:** 管理画面と同期基盤が動作

- [ ] `W3-1` **[AI]** 管理画面の保護（Basic認証）+ noindex + robots disallow
- [ ] `W3-2` **[AI]** 管理：同期実行 / 期間指定backfill / 結果・エラー・再実行（`sync_runs`）
- [ ] `W3-3` **[AI]** 管理：選手（基本情報・Wikidata対応・同姓同名確認）
- [ ] `W3-4` **[AI]** 管理：スタッツ（API値の確認・手動override・変更履歴）
- [ ] `W3-5` **[AI]** 管理：チーム（franchise・名称・カラー・移転/改称）
- [ ] `W3-6` **[AI]** 管理：ランキング規則（シーズン・指標別minimum）
- [ ] `W3-7` **[AI]** 管理：SEO（index / noindex / canonical）
- [ ] `W3-8` **[AI]** 管理：問い合わせ（メール転送方式の場合は送信ログのみ）
- [ ] `W3-9` **[AI]** override実効ビューの結線 + integrationテスト → **T7（同期で手動修正が消えない）**
- [ ] `W3-10` **[AI]** ランキング規定の公式例外 + 短縮シーズン比例 + unitテスト → T5
- [ ] `W3-11` **[AI→承認]** **法務6ページの草案作成**（利用規約 / プライバシー / データ出典 / 画像クレジット / 免責 / 訂正方針）
      ※オーバーライドの計画ではWeek 5ですが、**Week 3へ前倒しを提案します**。理由は下の「計画からの変更提案」
- [ ] `W3-12` **[オーナー]** 管理画面の操作確認 + ランキング最低条件の妥当性チェック（45分）← **9/27 チェックポイント**

---

## Phase 3（API連携）— **10/4 公開スコープに戻しました（2026-09-06 オーナー判断）**

> **9/5 に「時間が無いので公開後に回す」と決めましたが、9/6 に取り消しました。**
> 理由: seed を実在選手の名前で作ると、**AIが思い出した誤った数字を実在の人物の記録として公開する**
> ことになります。「サンプルデータ」と明示しても誤情報である事実は変わりません。
> 実データを入れればこの問題は根本から消えます。→ [DECISIONS.md](./DECISIONS.md) §12

**代わりに削るもの**（§削減ラダー の1番・2番をそのまま適用）:

- 画像パイプライン（`W5-1`〜`W5-3` `W5-5`）→ 公開後。全選手を代替表示で公開
- チーム比較（`W2-9`）→ 公開後。チーム詳細と年代別ロスターは残す
- **選手比較・選手詳細・選手一覧は削りません。** ここが中核仮説

**同期を動かす場所:** GitHub Actions。
AIの作業環境から外部APIへ接続できない（プロキシが遮断する）ため、
取得はCI上で行い、手元では記録した応答を使って検証します。

**⏳ オーナー作業:** BALLDONTLIE の APIキーを GitHub Secrets（`BALLDONTLIE_API_KEY`）に登録。
キーが無い間、同期ワークフローは**緑のまま何もせず終了**します（マイグレーションと同じ作り）ので、開発は止まりません。
**契約プランで成績データまで取れるかは要確認です。お金が絡むので私は決めません。**

- [ ] `W4-1` **[AI]** `BalldontlieProvider` 実装（レート制限 / ページング / リトライ）
- [ ] `W4-2` **[AI]** 同期ジョブ：players / stints / player_stats / team_stats
- [ ] `W4-3` **[AI]** 冪等性のintegrationテスト（同じ期間を2回流して重複ゼロ）→ **T8**
- [ ] `W4-4` **[AI]** backfill（シーズン範囲指定）
- [ ] `W4-5` **[AI]** Vercel Cron による日次同期 + 失敗通知
- [ ] `W4-6` **[AI]** `provider_entity_ids` によるID解決・同姓同名マッチング + テスト
- [ ] `W4-7` **[オーナー]** **API実データの抜き取り検証（1〜2h）★AIには代替できない作業**
      移籍選手のstint分割 / 欠損のNULL・N/A表示 / **チーム成績が個人合計になっていないか**
- [ ] `W4-8` **[AI→承認]** 法務ページの内容確定（`W3-11` の承認内容を反映）
- [ ] `W4-9` **[AI]** 全ページへのデータ出典・最終更新日時の表示
- [ ] `W4-10` **[AI]** 実データ公開の可否判定レポート作成（実データで行くか、seed公開のままか）
- [ ] `W4-11` **[オーナー]** 実データ公開の可否判定（15分）

---

## Sprint 4 — 9/28〜10/4（法務 + QA + 公開）

**完了条件:** 公開URL稼働、被験者1名が操作

- [ ] `W5-1` **[AI]** 画像：Wikidata QID照合 → P18 → Commons imageinfo/extmetadata（**［ストレッチ］**）
- [ ] `W5-2` **[AI]** 画像：許可ライセンスのみ保存 + WebP/thumbnail生成 + Storage（**［ストレッチ］**）
- [ ] `W5-3` **[AI]** 画像：管理画面の審査UI（人物一致 / approve / reject）（**［ストレッチ］**）
- [ ] `W5-4` **[AI]** **未承認選手の代替表示（背番号・イニシャル・チームカラー）★これは削減しない**
- [ ] `W5-5` **[オーナー]** 画像の人物一致承認 上位50〜100名（2〜3h）（**［ストレッチ］**）
- [ ] `W5-6` **[AI]** 法務6ページの実装 + フッターからの常時リンク
- [ ] `W5-7` **[AI]** 問い合わせ4窓口（広告PR協賛 / 一般 / 不具合 / 権利訂正）
- [ ] `W5-8` **[AI]** E2Eテスト（PC / モバイルの主要フロー）→ T1〜T11
- [ ] `W5-9` **[AI]** パフォーマンス（LCP 2.5秒以内）・アクセシビリティ（keyboard / focus / alt / contrast / 表見出し）
- [ ] `W5-10` **[AI]** 「データ準備中のサンプル・非公式サイト」表示（seed公開の場合のみ）
- [ ] `W5-11` **[AI]** 本番Supabaseへのマイグレーション適用 + 日次バックアップ設定の確認
- [ ] `W5-12` **[AI]** 本番デプロイ + 公開前チェックリスト消化
- [ ] `W5-13` **[オーナー]** **公開判断（30分）**
- [ ] `W5-14` **[オーナー]** **被験者1名への連絡・操作依頼・結果確認（30分）← 10/4 ゴール**

---

## オーナー作業の合計

**残っているオーナー作業のみ（Week 0 分は消化済み）。**

| 期間 | 時間 | 内訳 |
|---|---|---|
| Sprint 1（9/5〜9/13） | **1.2h** | Vercelデプロイ15分 / Node確認15分 / Phase 0レビュー承認 `W1-12` 60分 |
| Sprint 2（9/14〜9/20） | 1.5h | モック突き合わせ×2 / seed数値の抜き取り確認 |
| Sprint 3（9/21〜9/27） | 0.75h | 管理画面の操作確認 / 法務6ページの承認 |
| Sprint 4（9/28〜10/4） | 1h | 公開判断30分 / 被験者への連絡30分 |
| **合計** | **約4.5時間**（画像を実施する場合 +2〜3h） | Phase 3 を公開後に回したため、実データ検証1〜2hが公開スコープから外れました |

**最も重いのは `W1-12`（Phase 0成果物の承認・60分）です。** ここだけは丁寧にお願いします。後から変えると作り直しになる唯一の箇所です。

---

## 計画からの変更提案（[提案・要承認]）

オーバーライド §3 の週割りに対し、**1点だけ変更を提案します。無断では実行しません。**

**法務6ページの草案作成を Week 5 → Week 3 へ前倒しする（`W3-11`）**

- 理由1: 法務ページは**公開の必須条件**であり、遅延の影響を受けてはいけない。Week 5に置くと、
  画像パイプラインやQAと同じ週で競合し、最も落としてはいけないものが最も混雑した週に入る。
- 理由2: 一人開発の原則として「終盤にはAIタスクだけが残る構成」にしたい。法務は `[AI→承認]` なので、
  オーナーの承認待ちが発生する。**承認待ちは早い週に置くほど安全。**
- 影響: Sprint 3 のAIタスクが1つ増えるが、Sprint 3は管理画面中心で並行しやすい。10/4は動かない。
- **2026-09-05 追記: この前倒しは承認済みとして実行します。** Phase 3 を外したぶん Sprint 3 に余裕ができたため。

---

## 削減ラダー（チェックポイント未達時、上から順に削る）

**日付ではなく機能を削る。**

1. 画像パイプライン（`W5-1`〜`W5-3`, `W5-5`）を丸ごと公開後に回す → 全選手を代替表示（`W5-4`）で公開
2. チーム比較（`W2-9`）を公開後に回す（チーム詳細・年代別ロスターは残す）
3. ランキング（`W2-10`, `W3-6`, `W3-10`）を公開後に回す（`ranking_rules` のスキーマは残す）
4. 検索の詳細絞り込みを基本条件のみに縮小（成績範囲指定・ドラフト条件を後回し）
5. 36分換算・eFG%・TS% を後回し（平均／合計は残す）
6. **選手比較・選手詳細・選手一覧は最後まで削らない。ここが中核仮説。**

## 運用ルール

- **セッション再開時は必ずこのファイルを読み**、完了タスクにチェックを入れ、現在地を1行報告してから作業に入る
- 完了報告には必ず「進捗: 完了n / 全N」と「次にオーナーがやること: 最大3つ・所要時間つき」を書く
- タスクが増えたら、このファイルに追記した上で**10/4への影響を必ず報告する**。黙って総量を増やさない
- 各フェーズ完了時点で**常にデプロイ可能な状態を保つ**（統合問題を最後にまとめない）
