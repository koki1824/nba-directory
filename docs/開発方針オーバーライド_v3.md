# 開発方針オーバーライド v3

**対象:** NBA選手名鑑 / Claude Code開発
**上位文書:** `NBA選手名鑑_ClaudeCode開発要件定義書_更新版.docx`（v1.0 / 2026-07-26）
**位置づけ:** 更新版要件定義書は唯一のプロダクト要件として有効。本書は**進め方（順序・完了定義・オーナー作業配置）**を規定する。機能仕様が矛盾する場合は要件定義書が優先。進行順序が矛盾する場合は本書が優先。
**公開日:** **2026年9月1日**（固定。延期しない）
**方針:** 公開日までに載せられる機能を最大化する。仮枠を作って後で捨てる進め方を禁止する。

*v2からの変更: 公開日を8/15→9/1へ変更。「fixtureファイルで進めDBは後回し」という方針を**撤回**し、初日から本番スキーマでDBを構築する方針に修正。ランキング・チーム比較を初回スコープに復帰。*

---

## 0. 基本原則

### 0.1 仮枠を作らない

オーナーの明確な要求である。以下を厳守する。

- **DBは初日から本番スキーマで作る。** fixtureは「ファイル」ではなく「**DBへ投入するseed**」として持つ。フラットファイルでデータ層を仮組みし、後でDBへ移行する進め方を禁止する
- API接続時に発生する作業は「同期処理の追加」のみとし、データ層の作り直しが発生しない構造にする
- 型・スキーマ・インターフェースは、後から追加する機能を見越して**最初に確定**させる

### 0.2 「壊れない」は設計完了で担保する（実装完了では担保できない）

後から機能を追加して壊れる原因は、契約（スキーマ・インターフェース・URL・認証境界・欠損値表現）が未確定なことであり、実装が途中であることではない。したがって:

- **Phase 0で全機能分の契約を確定する**（実装しない機能の分も含めてスキーマとインターフェースを引く）
- **実装は縦スライスで行う**。各スライスは仮組みではなく完成品として仕上げ、DB・型・デザインは本番と同一
- 統合の問題を最後にまとめて出さない。各フェーズ完了時点で常にデプロイ可能な状態を保つ

### 0.3 遅延時の調整方向

- スコープ調整は**削る方向のみ**許可する。**9/1を動かす調整を禁止する**
- 削減は §5 の削減ラダーの順に行う

---

## 1. 公開の定義

- 公開 ＝ 「**公開URLが存在し、実在のNBAファン1名が実際に選手比較機能を操作した日**」
- 期日: **2026年9月1日**
- 全機能の完成を公開条件にしない。9/1時点で載っていない機能は、公開後に追加する

---

## 2. 9/1公開スコープ

### 2.1 9/1に載せる

| フェーズ | 内容 | 確度 |
|---|---|---|
| Phase 0 | 環境・**全機能分のDBスキーマ**・Provider Adapter・URL構成・デザイントークン | 確実 |
| Phase 1 | UI全体（トップ／選手一覧／選手詳細／**選手比較**／チーム一覧・詳細・年代別ロスター／チーム比較／ランキング）+ モバイル設計 | 確実 |
| Phase 2 | migration / seed / 内部ID / manual override / sync logs / 管理画面 / ランキング規定 | 確実 |
| Phase 3 | Provider Adapter実装・日次同期・stint分割・backfill | **API契約が8/10までにクリアすれば** |
| Phase 4 | 画像（Wikidata / Commons / license allowlist / 審査UI）。**承認は上位50〜100名のみ** | ストレッチ |
| 法務・公開準備 | 利用規約・プライバシーポリシー・データ出典・画像クレジット・免責・訂正方針 | 確実（公開に必須） |
| 問い合わせ | 4窓口（広告PR協賛／一般／不具合／権利訂正） | 確実 |

### 2.2 9/1見送り（公開後に追加）

| 項目 | 見送り理由 |
|---|---|
| **WordPress / コラム統合** | 同一ドメインルーティング（reverse proxy等）は公開直前に入れると最も事故る。データアプリと疎結合なので、後から足しても既存を壊さない |
| **会員・お気に入り** | 認証・退会・データ削除・プライバシー対応まで含めると重い。公開後に追加してもDB設計上壊れない（favoritesテーブルはPhase 0でスキーマだけ引いておく） |
| SEO作り込み・構造化データ・sitemap最適化 | 公開が先。初回はcanonical + 自動比較noindexのみ |
| 画像の全選手承認 | 未承認選手は代替表示（背番号・イニシャル・チームカラー）で公開 |
| 引退選手の完全収録 | 要件定義書でもMVP対象外 |
| 独自スコア | 要件定義書で［保留］ |

**重要:** 見送る機能も、**Phase 0でDBスキーマとインターフェースは引いておく**（favorites、data_sources、SEO制御フラグ等）。後から追加してもマイグレーションが破壊的にならないようにする。これが「後から壊れない」の実体である。

---

## 3. フェーズ計画（7/27 → 9/1）

| 週 | 期間 | フェーズ | 完了条件 |
|---|---|---|---|
| Week 0 | 7/27–7/29 | **オーナー前倒しタスク全消化** + Phase 0着手 | アカウント・API契約確認送付・［保留］決定が完了 |
| Week 1 | 7/30–8/5 | Phase 0完了 → Phase 1着手 | スキーマ・Providerインターフェース・URL・トークンをオーナー承認 |
| Week 2 | 8/6–8/12 | Phase 1 | 主要画面がseedデータで動作。モバイル含む |
| Week 3 | 8/13–8/19 | Phase 2 | 管理画面・override・sync logs・ランキング規定が動作 |
| Week 4 | 8/20–8/26 | Phase 3 | API同期が冪等に動作。実データ検証済み |
| Week 5 | 8/27–9/1 | Phase 4（部分）+ 法務 + QA + **公開** | 公開URL稼働、被験者1名が操作 |

**チェックポイント（未達なら §5 の削減を即実行）**
- **8/5**: Phase 0承認済み・Vercelに空アプリがデプロイ済み
- **8/12**: 選手比較がseedデータで動作
- **8/19**: 管理画面と同期基盤が動作
- **8/26**: API実データが入っている、または「入らない」と確定してseed公開に切替済み

---

## 4. オーナー作業カレンダー

**総量: 5週間で18〜24時間（日次30分相当）。うち約1/4がWeek 0に集中する。**

### Week 0（7/27–7/29）— 約4〜5時間 ★最重要

| 作業 | 時間 | 備考 |
|---|---|---|
| **BALLDONTLIE契約条件の書面確認** | 1〜2h + 返信待ち | **最長納期。§6のリスク参照** |
| Supabaseアカウント＋プロジェクト作成 | 20分 | DB URL / anon key / service role key を控える |
| Vercelアカウント作成 | 10分 | GitHubログイン可 |
| GitHubリポジトリ作成 | 15分 | |
| Node.js 20以上のインストール確認 | 15分 | |
| **［保留］項目の決定** | 30分 | 下記 |

**Week 0で決める［保留］項目:**
- 季節年齢の基準日（開幕日 or 固定日）
- 比較上限人数（MVP 2人 / 3〜4人）
- 仮サイト名（slug・ブランディング固定用。**ドメイン購入は不要**、vercel.appで公開）
- プレーオフランキングの表示ルール（公式minimumがない指標の扱い）

**後回し可:** 独自スコア、引退選手の代表色アルゴリズム、最終ロゴ、正式ドメイン

### Week 1–5

| 時期 | 作業 | 時間 |
|---|---|---|
| Week 1末 | **Phase 0成果物レビュー**（DBスキーマ・Providerインターフェース・URL構成・デザイントークン）。**後から変えると壊れる唯一の箇所。ここだけは丁寧に** | 60分 |
| Week 2 | UI実装をモック7枚と突き合わせ、差分を指摘 | 30分×2 |
| Week 3末 | 管理画面の操作確認、ランキング最低条件の妥当性チェック | 45分 |
| Week 4 | **API実データの抜き取り検証**（移籍選手のstint分割、欠損のNULL/N-A表示、チーム成績が個人合計になっていないか） | 1〜2h |
| Week 5 | 画像の人物一致承認（上位50〜100名）／法務ページ確認／公開判断／被験者への連絡 | 2〜3h |

### オーナーにしか実行できない作業の分類

以下の4種のみがオーナー作業である。これ以外がオーナーに割り当てられていたら設計ミスとして指摘すること。

1. アカウント作成・課金・認証情報の発行
2. ［保留］項目の意思決定
3. **実データが現実と合っているかの検証**（AIは自分の出力が事実か判定できない）
4. 画像の人物一致判定・公開可否判断・外部への連絡

---

## 5. 削減ラダー（遅延時はこの順に削る）

チェックポイント未達時、**日付ではなく機能を削る**。上から順に実行する。

1. 画像パイプライン（Phase 4）を丸ごと公開後に回す → 全選手を代替表示で公開
2. チーム比較を公開後に回す（チーム詳細・年代別ロスターは残す）
3. ランキングを公開後に回す（規定処理が最も複雑。ranking_rulesのスキーマは残す）
4. 検索の詳細絞り込みを基本条件のみに縮小（成績範囲指定・ドラフト条件を後回し）
5. 36分換算・eFG%・TS%を後回し（平均／合計は残す）
6. **選手比較・選手詳細・選手一覧は最後まで削らない。ここが中核仮説**

---

## 6. 最大のリスク: API契約条件

**BALLDONTLIE等が「自社DBへの永続保存」を許可しない場合、15テーブル前提のスキーマ設計が崩れ、キャッシュ主体の設計に作り直しとなる。これは9/1を確実に落とす。**

- **Week 0中に、書面（規約文面またはメール回答）で確認すること**: 商用表示可否 / 自社DB保存可否 / キャッシュ保持期間 / 再配布 / 解約後のデータ保持 / 出典表示義務 / レート制限 / 取得可能な年度範囲
- 回答が8/10までに得られない、または保存不可の場合:
  - Phase 3を9/1スコープから外す
  - **seedデータ（実在選手20〜30名の実数値）で公開する**
  - サイト上に「データ準備中・非公式」を明示
  - 代替プロバイダの調査をPhase 0と並行して実施
- **契約前に本番データ永続化を確定しない**（要件定義書4.1の［保留／ブロッカー］は有効）

---

## 7. seedデータ要件（API接続前・および接続失敗時の公開用）

- 現役選手 **20〜30名**、**3チーム以上**、複数シーズン
- 必ず含める境界ケース: シーズン途中の移籍（stint分割）/ 同一選手の複数シーズン / 欠損項目あり（NULL・N/A・Not calculatedの出し分け）/ RSとPO両方ある選手とPO未出場の選手 / 同姓同名候補
- **数値の正確性はオーナーが抜き取り確認する。** AIが生成した推測値をそのまま公開しない
- 公開時、サイト上に「データ準備中のサンプル・非公式サイト」を明示
- 大量スクレイピングは行わない

---

## 8. 進行ルール

- 1フェーズずつ実装。各フェーズ開始前に計画を提示し、承認を得てから着手
- **各フェーズ完了時点で常にデプロイ可能な状態を保つ**（統合問題を最後にまとめない）
- 既存ファイルを無断で削除・上書きしない
- DB変更は必ずマイグレーション化する
- データ取得を特定APIに直結させず、必ずProvider Adapterを介す
- **0 / NULL / N/A / Not calculated を区別する。欠損値を0扱いしない**
- キャリア値をシーズン率の単純平均で算出しない
- チーム成績を個人成績の合計で代用しない
- secretは環境変数管理。フロントに露出しない
- SQLはプリペアド処理。サニタイズ・エスケープ・権限チェックを徹底
- デザインはA改良版基準（背景 #F7F4EE / メイン #0F1D2D / アクセント #B23A2E）。紫グラデーション、過剰な角丸カード、glassmorphism、AI生成感の強いUIを避ける
- 公式NBAロゴ・チームロゴ・権利未確認画像を使用しない
- モバイルはPC版の縮小ではなく情報順序を再設計する
- 依存追加前に目的・保守状況・ライセンスを説明する
- ［保留］項目を勝手に確定しない

### フェーズ完了報告フォーマット

```
## 完了報告: [フェーズ名]
✅ できたこと
📍 進捗: ROADMAP 完了n / 全N（9/1まで残り◯日 / 残り◯タスク）
🚦 チェックポイント判定: 予定通り / 遅延（遅延時は削減ラダーの何番を提案するか）
🧪 テスト結果
🔗 現在のデプロイURL
⚠️ 未解決・保留
🙋 次にオーナーがやること: 最大3つ、各所要時間付き
🤖 次にDispatch / Claude Codeへ投げるタスク: コピペで投げられる指示文
```

---

## 最初の指示（Claude Codeへ）

まだ実装しない。更新版要件定義書と本オーバーライドを読み、以下を日本語で提示すること。

1. 要件理解の要約と、9/1公開スコープの確認
2. 既存環境の調査結果
3. 推奨ディレクトリ構成（Next.js + TypeScript + Supabase）
4. **全機能分のDBスキーマ案**（9/1に実装しないfavorites等も含む。主キー・インデックス・provider_entity_ids・manual_overrides・欠損値表現を明示）
5. Provider Adapterインターフェース案と型定義案
6. URL構成案
7. **`docs/ROADMAP.md` 案**（9/1公開まで。担当ラベル [AI] / [AI→承認] / [オーナー] 付き。**[オーナー]タスクはWeek 0に前倒し**）
8. 9/1に対する実現可能性の率直な評価。溢れる場合は削減ラダー（本書§5）に沿った提案
9. テスト戦略
10. デプロイ手順案（Vercel、独自ドメインなし）
11. 未確定事項・実装前に確認が必要な事項
12. 技術リスクと代替案

曖昧さ・矛盾は勝手に補完せず質問すること。ただし技術的細部でユーザー判断が不要なものは、推奨案と理由を提示すること。
