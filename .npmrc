# サポート外のNodeでは install 時点で止める。
# これが無いと engines は単なる警告で、jsdom等が実行時に不可解なクラッシュをする。
#
# engines は "22.x" と書いてあります（>=22.22.2 のような範囲だと Vercel が
# 使う Node を判断できず、既定の Node でビルドして失敗するため）。
# パッチ版の下限（22.22.2）は src/test/setup.ts で見張っています。
engine-strict=true
